# Knowledge-as-Code Pattern Site

This directory contains the standalone landing page for [knowledge-as-code.com](https://knowledge-as-code.com).

It is **separate from the template's generated output** in `docs/`. The template build (`node scripts/build.js`) does not modify this directory.

This page is deployed independently and serves as the canonical definition of the Knowledge as Code pattern.
